import {
   // Asynchronous Actions
   fetchCallReports,
   fetchPeriodDates,
   fetchFilers,

   // Synchronous Actions
   selectPeriodDate,
   selectFiler,
} from './fdic';

import {
  // Asynchronous Actions
  fetchNews,
} from './news';

export default {
  /**
   * FDIC
   */

  // Asynchronous Actions
  fetchCallReports,
  fetchPeriodDates,
  fetchFilers,

  // Synchronous Actions
  selectPeriodDate,
  selectFiler,

  /**
   * NEWS
   */

  // Asynchronous Action
  fetchNews,
}

